name: 'Setup apptainer'
description: 'Setup apptainer an open source container platform'
author: Stefan Verhoeven
inputs:
  apptainer-version:
    description: Version of singularity to setup
    required: false
    default: 1.1.2
runs:
  using: 'node20'
  main: 'dist/index.js'
branding:
  icon: box
  color: blue
